# Database
DB_NAME=attendance
DB_USERNAME=attendance
DB_PASSWORD=attendance
DB_ROOT_PASSWORD=rootpw

# Backend
SPRING_PROFILES_ACTIVE=dev
JWT_SECRET=replace-me-with-at-least-32-bytes-of-random-data
CORS_ORIGINS=http://localhost:5173,http://localhost:8080
COOKIE_SECURE=false

# Bootstrap admin (created on first boot if not present)
ATTENDANCE_BOOTSTRAP_ADMIN_USERNAME=admin
ATTENDANCE_BOOTSTRAP_ADMIN_PASSWORD=Admin@12345
ATTENDANCE_BOOTSTRAP_ADMIN_EMAIL=admin@local
